---
interface Props{
    date: string
    title: string
    description: string
}

const {date, title, description}=Astro.props
---
<div class="absolute w-3 h-3 bg-[var(--gris5)] rounded-full mt-1.5 -start-1.5 border border-[var(--gris-5)] opacity-90"></div>
        <time class="mb-1 text-sm font-normal leading-none text-gris3 opacity-55">{date}</time>
        <h3 class="text-lg font-semibold text-gris6">{title} </h3>
        <p class="mb-4  font-normal text-gris3 text-[14px] text-balance" set:html={description}></p>
    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
  </svg></a>